<script type="text/javascript">
  $(document).ready(function(){
      // $('.lights_out_picker').datepicker({});
  })
</script>

<%= form_for(@diary_entry) do |f| %>
  <% if @diary_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@diary_entry.errors.count, "error") %> prohibited this diary_entry from being saved:</h2>

      <ul>
      <% @diary_entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :lights_out %><br>
    <%= f.text_field :lights_out_formatted, :class => "lights_out_picker" %>
  </div>
  <div class="field">
    <%= f.label :wakeup %><br>
    <%= f.text_field :wakeup_formatted, :class => "lights_out_picker" %>
  </div>
  <div class="field">
    <%= f.label "Minutes awake" %><br>
    <%= f.text_field :awake_duration %>
  </div>

  <div class="field">
    <%= f.label :notes %><br>
    <%= f.text_area :notes, :rows => "10" %>
  </div>
  <div class="field">
    <%= f.label :rating %><br>
    <%= f.number_field :rating %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
